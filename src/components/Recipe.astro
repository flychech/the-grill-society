<div class="h-screen w-full bg-black bg-grill-coal bg-no-repeat bg-cover text-white py-20">
  <div class="w-full max-w-7xl m-auto flex gap-8 h-full">
    <div class="flex flex-col justify-center w-1/2">
      <div class="recipe-text p-16 border-dashed border-white border-2 border-spacing-1">
        <h1 class="text-4xl font-bold main-title mb-10">RECETA DE COSTILLAS AHUMADAS</h1>
        <p class="text-2xl">
          Lorem ipsum dolor sit amet consectetur adipisicing elit. Laudantium consequuntur aliquid
          facere quibusdam natus? Repudiandae, cupiditate! Ea facere sunt, cum corrupti laudantium,
          magni accusantium, repudiandae maxime sapiente dolorem ullam tempore!
        </p>
      </div>
    </div>

    <div class="flex w-1/2 h-full">
      <div class="card">
        <div class="recipe-video">
          <video
            loop
            preload="auto"
            controls
            class="w-full h-full"
            poster="../../assets/banner.jpg"
          >
            <source type="video/mp4" src="../../assets/recipe.mp4" />
            Yout browser does not support the video tag.
          </video>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .recipe-text {
    border-radius: 8px;
    backdrop-filter: grayscale(1);
  }

  .card {
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
  }

  .card::before {
    content: '';
    position: absolute;
    inset: 0;
    left: -3.5px;
    margin: auto;
    width: calc(100% + 8px);
    height: calc(100% + 8px);
    border-radius: 10px;
    background: linear-gradient(-45deg, #efdd03 0%, #f94616 100%);
    pointer-events: none;
    transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .card::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(-45deg, #efdd03 0%, #f94616 100%);
    transform: translate3d(0, 0, 0) scale(0.95);
    filter: blur(20px);
    transition: filter 0.4s;
  }

  .recipe-video {
    background-color: #000;
    z-index: 1;
    color: white;
    border-radius: 8px;
    width: 100%;
    display: flex;
    flex-direction: column;
    justify-content: end;
    gap: 12px;
  }
  .heading {
    font-size: 20px;
    text-transform: capitalize;
    font-weight: 700;
  }

  .card p:not(.heading) {
    font-size: 14px;
  }

  .card p:last-child {
    color: #e81cff;
    font-weight: 600;
  }

  .card:hover::after {
    filter: blur(90px);
  }

  /* .card:hover::before {
    transform: rotate(-90deg) scaleX(1.47) scaleY(0.69);
  } */
</style>
